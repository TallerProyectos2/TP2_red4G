# Validacion de la fase 0 del EPC

- Fecha: `2026-03-04`
- Jira: `TP2-141`
- Maquina: `tp2-EPC`

## Evidencia de ejecucion

- `hostnamectl --static` devolvio `tp2-EPC`
- `ip -4 -o addr show` confirmo:
  - `enp1s0` -> `10.10.10.1/24`
  - `srs_spgw_sgi` -> `172.16.0.1/24`
  - `tailscale0` -> `100.97.19.112/32`
- `nmcli -f ipv4.method,ipv4.addresses connection show 'Conexion cableada 2'` devolvio `manual` y `10.10.10.1/24`
- `nmcli -f ipv4.method,ipv4.addresses connection show 'srs_spgw_sgi'` devolvio `manual` y `172.16.0.1/24`
- `ping -c 2 -W 1 10.10.10.2` tuvo exito con `0%` de perdida de paquetes y `0.309 ms` de RTT medio
- `srsepc --version` expuso el commit de compilacion `6bcbd9e5b`

## Cambio aplicado

- Se crearon los directorios de trabajo de la fase 0 del EPC en el host real:
  - `/srv/tp2`
  - `/srv/tp2/frames`
  - `/srv/tp2/logs`
  - `/srv/tp2/docker`
  - `/srv/tp2/config`
- Se asigno todo el arbol `/srv/tp2` a `tp2:tp2` con modo `755`

## Resultado

La linea base de la fase 0 del EPC queda ahora fijada y documentada: el host tiene las direcciones estaticas requeridas del lado EPC, el backhaul con el eNodeB responde y el arbol de trabajo esperado `/srv/tp2` existe y es escribible para las fases posteriores.
