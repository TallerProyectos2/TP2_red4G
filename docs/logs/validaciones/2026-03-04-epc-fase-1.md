# Validacion de la fase 1 del EPC

- Fecha: `2026-03-04`
- Jira: `TP2-142`
- Maquina: `tp2-EPC`

## Estado inicial

- `srsepc` ya se estaba ejecutando manualmente en el EPC con el comando `sudo srsepc /home/tp2/.config/srsran/epc.conf`
- `epc.conf` seguia conteniendo una ruta relativa a la base de datos HSS:
  - `db_file = user_db.csv`
- El log activo del EPC de `2026-03-03` mostraba el efecto secundario de esa ruta relativa bajo un proceso iniciado como root:
  - `Opened DB file: /root/.config/srsran/user_db.csv`

## Cambio aplicado

- Se hizo copia de seguridad de la configuracion activa del EPC en `/srv/tp2/config/epc.conf.pre-fase1-20260304`
- Se actualizo `/home/tp2/.config/srsran/epc.conf` para:
  - `db_file = /home/tp2/.config/srsran/user_db.csv`
  - `filename = /srv/tp2/logs/srsepc.log`
- Se realizo un reinicio controlado solo del EPC:
  - se detuvo el `srsepc` existente
  - se inicio `srsepc /home/tp2/.config/srsran/epc.conf`

## Evidencia de ejecucion

- `grep -n '^db_file' /home/tp2/.config/srsran/epc.conf` ahora devuelve la ruta absoluta a `user_db.csv`
- `sed -n` confirmo:
  - `mme_bind_addr = 10.10.10.1`
  - `gtpu_bind_addr = 10.10.10.1`
  - `sgi_if_addr = 172.16.0.1`
- `sed -n` sobre `/home/tp2/.config/srsran/user_db.csv` confirmo al menos dos UEs aprovisionados: `ue1` y `ue2`
- `pgrep -af srsepc` despues del reinicio devolvio un unico proceso activo:
  - `30003 srsepc /home/tp2/.config/srsran/epc.conf`
- `ss` confirma que los listeners del EPC estan activos:
  - `10.10.10.1:36412/SCTP`
  - `10.10.10.1:2152/UDP`
- `lsof -p 30003` muestra `/srv/tp2/logs/srsepc.log` abierto por el proceso en ejecucion
- La evidencia historica de S1 sigue presente en el log del EPC:
  - `Received S1 Setup Request`
  - `Sending S1 Setup Response`
  - par `srsenb01`
- Una nueva ventana de observacion de 15 segundos tras el reinicio del EPC no produjo nuevas lineas de configuracion S1, por lo que no se observo ningun nuevo intento de conexion del eNodeB durante esta ventana de validacion

## Resultado

El lado EPC de la fase 1 es ahora consistente y seguro frente a reinicios: el proceso activo del core LTE se esta ejecutando con la ruta absoluta corregida para la base de datos HSS, las direcciones requeridas del lado EPC estan enlazadas y el handshake S1 previamente exitoso queda preservado en la evidencia operativa. El reestablecimiento fresco de S1 depende de que el eNodeB inicie una nueva conexion.
