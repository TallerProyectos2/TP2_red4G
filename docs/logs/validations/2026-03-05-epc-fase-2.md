# EPC Phase 2 Validation

- Date: `2026-03-05`
- Jira: `TP2-143`
- Machine: `tp2-EPC`

## Initial State

- `srsepc` and `srsenb` were running manually and S1 setup was active
- EPC had no UE routing enabled:
  - `net.ipv4.ip_forward = 0`
- NAT for `172.16.0.0/24` was missing
- `dnsmasq` binary existed but no active `dnsmasq` service was configured

## Applied Change

- Backed up EPC config to `/srv/tp2/config/epc.conf.pre-fase2-20260305`
- Updated `/home/tp2/.config/srsran/epc.conf`:
  - `dns_addr = 172.16.0.1`
- Enabled persistent IPv4 forwarding:
  - `/etc/sysctl.d/99-tp2-epc-routing.conf` with `net.ipv4.ip_forward=1`
- Added persistent UE routing bootstrap:
  - script: `/usr/local/sbin/tp2-ue-routing.sh`
  - systemd unit: `/etc/systemd/system/tp2-ue-routing.service`
  - enabled and started service
- Installed and configured DNS service:
  - installed `dnsmasq` package
  - config file `/etc/dnsmasq.d/tp2-epc.conf`
  - service `dnsmasq` started and active
- Restarted `srsepc` once to apply the EPC DNS parameter

## Runtime Evidence

- `sysctl -n net.ipv4.ip_forward` -> `1`
- `iptables` rules present:
  - `POSTROUTING -s 172.16.0.0/24 -o eno1 -j MASQUERADE`
  - `FORWARD` accept path between `srs_spgw_sgi` and `eno1`
- `dig +short @172.16.0.1 openai.com` returned valid A records
- `ping -I 172.16.0.1 -c 2 1.1.1.1` succeeded with `0%` packet loss
- NAT hit counter increased on the UE-source MASQUERADE rule
- Fresh S1 setup after EPC restart observed in `srsepc` console log
- eNodeB side confirms S1 `ESTAB` toward EPC (`10.10.10.1:36412`)

## Pending Check

No new UE attach/data-plane events were observed during the short post-change observation window. Final criterion requiring explicit UE-side ping/HTTP confirmation remains pending until the UE generates traffic through the EPC path.
